# Allow legacy peer deps to resolve Canvas version conflicts
legacy-peer-deps=true

# Auto install peer dependencies  
auto-install-peers=true

# Engine strict to ensure Node version compatibility
engine-strict=true